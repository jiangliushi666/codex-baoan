# Codex 保安设计风格

这份文档沉淀 Codex 保安当前最有辨识度的界面气质，用作后续页面、组件和物料扩展时的设计基准。判断标准以项目内的桌面截图 `codex-guard-ui.png` 为主：深色安全控制台、橙色行动焦点、低饱和边界、高密度本机监控信息。

## 风格定位

Codex 保安不是通用 SaaS 后台，也不是可爱化助手。它应该像一个本机值守台：安静、克制、响应快，关键风险和主操作一眼能看到。

核心关键词：

- 本机值守：强调“正在保护这台机器”，而不是远程云平台。
- 安全控制台：信息密度高，层级清楚，状态、来源、风险、记录并列呈现。
- 温暖警戒：主色使用橙色，像安全警示灯，但整体背景保持冷静。
- 少解释，多状态：界面不做教学页，用状态、计数、日志和按钮表达功能。
- 桌面应用感：固定侧栏、顶部状态区、抽屉设置、紧凑按钮，避免网页营销感。

## 视觉骨架

主布局采用“左侧导航 + 右侧工作区”。

- 左侧栏宽度保持在 230-250px，承载品牌、供应商/活动筛选、模式状态和设置入口。
- 右侧工作区按纵向扫描：顶部状态和主操作，中段告警/KPI，底部主要列表或详情。
- 首屏必须展示真实工作状态，不放欢迎页、英雄区、插画卡片或功能介绍区。
- 面板可以有边框和轻微阴影，但不能层层嵌套成卡片堆。
- 内容密度偏高，间距克制；关键按钮和风险状态用颜色拉开，而不是靠大标题。

## 色彩系统

推荐继续以深色优先。橙色是 Codex 保安的品牌动作色，蓝色只适合做兼容 cc-switch 或中性信息辅助色。

主色建议：

```css
--bg: #080a0f;
--rail: #0b0e14;
--panel: #171a22;
--panel-hover: #1d212b;
--border: #2a2e3a;
--border-strong: #363b48;
--text: #f7f7f8;
--text-muted: #a1a7b3;
--primary: #ff7a1a;
--primary-hover: #ff8c34;
--primary-soft: rgba(255, 122, 26, 0.14);
--danger: #ff5a5f;
--warning: #f7a928;
--success: #36c275;
--info: #4ea1ff;
--violet: #a78bfa;
```

使用规则：

- 橙色只给品牌标识、启动/扫描等主操作、当前模式、重点图标和强选中态。
- 危险事件用红色，不用橙色替代；橙色代表“保安正在介入”，红色代表“风险命中”。
- 背景不要做大面积渐变；局部品牌 mark 可以使用橙色渐变和内发光。
- 边框要可见但低饱和，避免纯黑纯白对撞。
- 浅色主题可以存在，但不应该成为主视觉记忆点。

## 字体和排版

整体是桌面工具字体系统，不追求品牌大字。

- 字体：`Inter`, `Segoe UI`, `-apple-system`, `system-ui`, `sans-serif`。
- 代码和路径：`Cascadia Code`, `JetBrains Mono`, `Consolas`, `ui-monospace`。
- 主标题：18-24px，700-800 weight。
- 区块标题：15-17px，700-750 weight。
- 正文/按钮：12.5-14px，550-650 weight。
- 辅助说明：11-12.5px，颜色使用 muted。
- 数字计数使用 `font-variant-numeric: tabular-nums`，保证日志和 KPI 稳定。

文字风格短、硬、明确。推荐“启动监控”“重新扫描”“风险命中”“没有来源”，避免“欢迎使用”“你可以在这里……”这类说明型文案。

## 组件语言

### 侧栏

侧栏是身份和导航，不是菜单列表堆砌。

- 品牌 mark 使用 40px 左右的圆角方形，橙色渐变，图标用盾牌或链路/守护相关符号。
- 筛选项使用小图标 + 两行文字/计数结构，当前项有低透明橙色底和左侧短竖条。
- 底部模式块固定在侧栏下方，强调当前保护策略或监控状态。
- 设置按钮是次级入口，视觉低于主操作。

### 顶部状态区

顶部状态区回答三个问题：当前保护谁、是否启用、下一步能做什么。

- 左侧显示上游来源、状态点、配置路径或模型信息。
- 右侧只放高频操作：重新扫描、启动/停止监控。
- 启动按钮是橙色实心；停止按钮使用红色描边或红色柔和背景。
- loading 状态使用旋转图标和简短状态文案。

### KPI 和告警

KPI 是筛选入口，不只是数据展示。

- 三到四个 KPI 足够：活动、文件、风险、网络或策略。
- 图标容器使用 36-40px 圆角方块，背景是对应状态色的透明色。
- 风险 KPI 有命中时用红色边框/浅红背景，保持克制但立即可见。
- 告警条靠近顶部，红色透明背景，文字可以换行，关闭按钮保持小尺寸。

### 列表和时间线

执行记录是产品核心，应保持可扫读。

- 每条记录使用左侧图标、标题、等级徽标、时间、摘要、命令/路径 chips。
- 高危记录用左侧红色边线和轻微红底，不要整条大红。
- 命令和路径用等宽字体，允许 `overflow-wrap: anywhere`。
- 空状态保持短句，不展示插画；一个图标、标题、一句说明即可。

### 设置抽屉

设置是右侧抽屉，不打断主界面上下文。

- 宽度约 400px，背景使用 popover 面板色。
- 主题选择用 segmented control，运行配置用 switch row。
- 更新卡片、日志目录入口属于设置内的操作行，不要提升为主屏 CTA。
- 抽屉打开时主界面加遮罩和轻微 blur。

## 图标规则

继续使用 `lucide-react`，统一线性图标。

- 常规 stroke width 2.0-2.4；品牌 mark 可略加粗到 2.5。
- 工具和动作按钮优先使用图标加短文字。
- 图标语义固定：盾牌=保护，刷新=扫描，播放=启动，方块=停止，警告三角=风险，终端=命令，眼睛=读取，文件加/减/笔=新建/删除/修改，地球=网络。
- 不新增手绘 SVG 图标，除非是品牌 mark 或第三方来源 logo。

## 动效和反馈

动效只用于确认状态，不做装饰。

- hover：边框加深、背景轻变、关键按钮轻微上移。
- active：按钮轻微缩放或回落。
- 在线状态点可使用低频 pulse。
- 抽屉 slide、toast rise、错误条 rise，时长控制在 160-240ms。
- 尊重 `prefers-reduced-motion`，关闭旋转和 pulse。

## 实现约束

后续改 UI 时优先复用 `src/styles.css` 的 token 和类名结构，避免每个组件内联颜色。

- 圆角：按钮/输入 8-9px，卡片/面板 11-14px，品牌 mark 10-12px。
- 主按钮高度 38px，小按钮 32px。
- 侧栏筛选项高度约 40px，图标容器 26px。
- 主面板 padding 14-18px；页面外边距 18-26px。
- 不使用大面积紫蓝渐变、毛玻璃圆球、营销 hero、嵌套卡片、过大的说明文案。
- 所有文本必须在 880px 和 560px 断点下可换行或省略，不能挤压按钮和状态徽标。

## 与当前代码的关系

当前 `src/styles.css` 已经有一套偏 shadcn/ui 的蓝色 token。若要恢复截图里的橙色 Codex 保安气质，优先替换 root/dark token 和品牌 mark 渐变，不必改组件结构。推荐把橙色版定为主品牌风格，蓝色保留为信息色或未来“跟随 cc-switch”主题。

